# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AIResults {
  actionItems: [ActionItem!]!
  attendees: [Attendee!]!
  decisions: [String!]
  detailedNotes: String
  followUpNotes: String
  meetingId: ID!
  summary: String!
}

type ActionItem {
  aiGenerated: Boolean!
  assignee: Attendee
  deadline: DateTime
  description: String
  id: ID!
  meetingId: ID!
  status: ActionItemStatus!
  title: String!
}

enum ActionItemStatus {
  DONE
  IN_PROGRESS
  OPEN
  UNKNOWN
}

input ActionItemsFilterDto {
  assigneeId: String
  meetingId: String
  search: String
}

type Attendee {
  aiGenerated: Boolean!
  email: String
  id: ID!
  meetingId: ID!
  name: String!
  role: AttendeeRole!
  userId: ID
}

enum AttendeeRole {
  ORGANIZER
  PARTICIPANT
  UNKNOWN
}

input ChangePasswordInput {
  currentPassword: String!
  newPassword: String!
}

input CreateActionItemDto {
  aiGenerated: Boolean!
  assigneeId: String
  deadline: DateTime
  description: String
  meetingId: String!
  title: String!
}

input CreateMeetingDto {
  description: String
  scheduledAt: DateTime!
  title: String!
  transcript: String
}

input CreateUserDto {
  email: String!
  firstName: String!
  lastName: String!
  password: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input DeleteActionItemDto {
  actionItemId: String!
  meetingId: String!
}

type Meeting {
  actionItems: [ActionItem!]!
  createdAt: DateTime!
  description: String
  findMeetingTranscript(meetingId: String!): Transcript
  id: ID!
  scheduledAt: DateTime!
  status: MeetingStatus!
  title: String!
  transcript: Transcript
  updatedAt: DateTime!
}

enum MeetingStatus {
  COMPLETED
  FAILED
  PENDING
  PROCESSING
}

type Mutation {
  addAttendee(addAttendeeDto: addAttendeeDto!): Attendee!
  changePassword(input: ChangePasswordInput!): Boolean!
  createMeeting(createMeetingInput: CreateMeetingDto!): Meeting!
  createNewActionItem(createActionItem: CreateActionItemDto!): ActionItem!
  createUser(createUserDto: CreateUserDto!): User!
  deleteAccount: Boolean!
  deleteActionItem(deleteActionItemDto: DeleteActionItemDto!): ActionItem!
  deleteAttendee(deleteAttendeeDto: deleteAttendeeDto!): Attendee!
  deleteMeeting(id: String!): Meeting!
  generateAIResults(aiInput: aiResultsDto!): AIResults!
  setPassword(input: SetPasswordInput!): Boolean!
  updateActionItem(updateActionItemDto: UpdateActionItemDto!): ActionItem!
  updateProfile(input: UpdateProfileInput!): User!
}

type PaginatedMeetings {
  meetings: [Meeting!]!
  pageNo: Int!
  pageSize: Int!
  totalCount: Int!
}

input PaginatedMeetingsDto {
  pageNo: Float!
  pageSize: Float!
  scheduledFrom: DateTime
  scheduledTo: DateTime
  search: String
  sortDateOrder: String
  status: MeetingStatus
}

enum Provider {
  GOOGLE
  LOCAL
}

type Query {
  findAll: [Meeting!]!
  findById(attendeeId: String!): Attendee!
  findMeeting(id: String!): Meeting!
  findUserMeetings(input: PaginatedMeetingsDto!): PaginatedMeetings!
  getAIResults(meetingId: String!): AIResults
  getActionItems(meetingId: String!): [ActionItem!]!
  getAttendees(meetingId: String!): [Attendee!]!
  getTranscript(meetingId: String!): Transcript
  getUserActionItemAssignees: [Attendee!]!
  getUserActionItems(filter: ActionItemsFilterDto): [ActionItem!]!
  getUserMeetings: [Meeting!]!
  me: User!
  users: [User!]!
}

input SetPasswordInput {
  newPassword: String!
}

type Transcript {
  content: String!
  id: ID!
  meetingId: ID!
}

input UpdateActionItemDto {
  actionItemId: String!
  assigneeId: String
  deadline: DateTime
  description: String
  meetingId: String!
  status: ActionItemStatus
  title: String
}

input UpdateProfileInput {
  firstName: String
  lastName: String
}

type User {
  avatar: String
  createdAt: DateTime!
  email: String
  firstName: String
  id: ID!
  lastName: String
  provider: Provider
}

input addAttendeeDto {
  aiGenerated: Boolean = false
  email: String
  meetingId: String!
  name: String!
  role: AttendeeRole = PARTICIPANT
  userId: String
}

input aiResultsDto {
  meetingId: String!
  transcript: String!
}

input deleteAttendeeDto {
  attendeeId: String!
}